<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="15dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="250dp"
        app:cardCornerRadius="20dp"
        android:id="@+id/post_item_cardview"
        android:elevation="10dp"
        android:layout_marginBottom="10dp">
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:id="@+id/post_item_imageview"
            android:src="@mipmap/ic_launcher"/>
    </androidx.cardview.widget.CardView>


    
    <ImageView
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:src="@drawable/ic_launcher_background"
        android:layout_below="@id/post_item_cardview"
        android:id="@+id/paw_icon"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textStyle="bold"
        android:text="Fido"
        android:layout_marginStart="5dp"
        android:layout_alignBottom="@id/paw_icon"
        android:layout_toRightOf="@id/paw_icon"
        android:id="@+id/post_item_title" />
    <ImageView
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:src="@drawable/ic_launcher_background"
        android:layout_below="@id/post_item_cardview"
        android:layout_alignParentEnd="true"
        android:id="@+id/like_icon"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:layout_marginStart="5dp"
        android:text="Another"

        android:layout_below="@id/paw_icon"
        android:id="@+id/post_item_subtitle" />
</RelativeLayout>